{
  "name": "hybrid-storage-contract",
  "version": "1.0.0",
  "description": "Smart contract that stores string and number values with ownership controls",
  "main": "index.js",
  "scripts": {
    "test": "npx hardhat test",
    "compile": "npx hardhat compile",
    "deploy:sepolia": "npx hardhat run scripts/deploy.js --network sepolia",
    "clean": "npx hardhat clean"
  },
  "keywords": ["ethereum", "solidity", "smart-contract", "hardhat"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.24.2"
  },
  "dependencies": {
    "dotenv": "^17.2.1"
  }
}